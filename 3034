#include <stdio.h>
#include <math.h>
int main(void)
{       
        int n,i;
        double a,b,c,temp;
        scanf("%d %lf %lf",&n,&a,&b);
        c=pow(a,2)+pow(b,2);
        c=sqrt(c);
        for(i=0;i<n;i++)
        {
                scanf("%lf",&temp);
                if(temp<=c)
                        printf("DA\n");
                else
                        printf("NE\n");
        }
        return 0;

}      
